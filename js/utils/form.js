// Приклад: js/utils/form.js
export function getFormValues(formElement) {
    // Отримання всіх значень форми
}

export function setFormValues(formElement, values) {
    // Встановлення значень полів форми
}

export function clearFormValues(formElement) {
    // Очищення значень форми
}

export function validateForm(formElement, rules) {
    // Валідація форми за правилами
}